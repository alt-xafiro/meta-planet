import { ABeeZee, Allerta } from 'next/font/google';

export const allerta = Allerta({
  subsets: ['latin'],
  display: 'swap',
  weight: '400'
});

export const aBeeZee = ABeeZee({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
  style: 'normal'
});
